---
- name: Prepare
  hosts: all
  vars_files:
    - vars.yml
  tasks:
    - name: Add the user jenkins
      ansible.builtin.user:
        name: jenkins
    - name: Creates directory
      file:
        path: /home/jenkins
        state: directory
        owner: jenkins
        group: jenkins
        mode: 0644